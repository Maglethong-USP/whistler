/*
	SCC0219 - Project Whistler

	Group:
		- Rafael Gallo
		- Andreas Munte
		- Guilherme Muzzi


	Angular module creation file.
	The Angularjs module must be created only once using the 
	'angular.module(name, [dependencies])' method.
	Future references should use the 'angular.module(name)' method.
*/

var myApp = angular.module('spicyApp', ['ngRoute']); 